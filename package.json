{
  "name": "root",
  "private": true,
  "scripts": {
    "start": "cd packages && cd react-app && yarn start",
    "bootstrap":"lerna bootstrap --hoist"
  },
  "devDependencies": {
    "lerna": "^3.20.2"
  }
}
